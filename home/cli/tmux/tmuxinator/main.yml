name: main
root: ~/

windows:
    - terrahoff:
          root: ~/projects/strise/terrahoff
          layout: main-vertical
          panes:
              - editor:
              - term:
    - auth0:
          layout: main-vertical
          root: ~/projects/strise/auth0
          panes:
              - editor:
              - term:
    - mito:
          layout: main-vertical
          root: ~/projects/strise/mito
          panes:
              - editor:
              - term:
    - prefect:
          layout: main-vertical
          root: ~/projects/strise/prefect
          panes:
              - editor:
                    - nix shell nixpkgs#poetry nixpkgs#python311Full -c 'fish -c "poetry shell"'
              - term:
                    - nix shell nixpkgs#poetry nixpkgs#python311Full -c 'fish -c "poetry shell"'
